# Images directory

This directory is retained to match the manual's required structure and to remain trackable in Git.

The original Lab 02 HTML referenced `images/event-banner.jpg`, but that file was absent from the inspected repository tree. The Lab 03 copy uses a clearly labelled text-only panel in the same figure area, with existing CSS classes. No substitute photograph, university logo, or invented original banner is included. There are no missing image requests.

The original Lab 02 files were not edited. An authentic banner can be added here later; only change the Lab 03 image markup after reviewing the layout again.
